package com.ojha.core

import MathUtil._

object NormalDistribution {

//  def probabilityMassFunction(mu: Double, sigmaSquared: Double, x: Double) = {
//    val sigma = math.sqrt(sigmaSquared)
//    (1 / sigma * math.sqrt(2 * math.Pi)) * math.exp(-1 * (math.pow(x-mu, 2) / (2 * sigmaSquared))
//  }
//
//  def cumulativeDistributionFunction(mu: Double, sigmaSquared: Double, x: Double) = {
//
//  }
}
